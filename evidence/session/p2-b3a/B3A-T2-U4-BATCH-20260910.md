# B3a Tier-2 U4 — batch8/16 與 resource endpoint（2026-09-10）

## U4 batch8 — qualified

固定 64x64、warmup=100、count=500、mode=1 immediate GetImage、reuse=1、
batch=8、cold=0。CPU-A → R3-A → CPU-B → R3-B；每 session 都由 holder
維持 X client，oracle 1514/1514 後執行 cell，telemetry exact-count `6319/6319`
（1519 + 600×8）。四個 cell 都 `CELLDONE`、expected=`00ff0000`；CPU
fallback=1（4800/4800），R3 fallback=0（4800/4800）。

| session | wall median (ms) | fallback |
|---|---:|---:|
| CPU-A | 2.518 | 1 |
| R3-A | 3.212 | 0 |
| CPU-B | 2.627 | 1 |
| R3-B | 3.025 | 0 |

同窗 paired ratio：R3-A/CPU-A=`1.276x`，R3-B/CPU-B=`1.151x`；aggregate
`1.216x`。這是描述性結果，不作跨 workload 架構裁決。

Resource endpoint 已改用實際 `termux-x11gpu com.waydefu.x11gpu :3` PID，
不是 launcher shell PID：

- CPU-A RSS 226100→223488→220560 kB，FD 87→87→86（T0/T1/T2）。
- R3-A RSS 227288→223084→220984 kB，FD 87→87→82。
- T3 均為 RSS=NA、FD=0、x3proc=0；每輪 `NO_X3_RESIDUE`。
- 首次 exploratory U4 batch8 的 endpoint 使用錯誤 launcher PID，僅保留原始
  記錄，不納入 resource aggregate；重跑的 `batch8-fix` 才是有效 endpoint 證據。

原始證據：`t2-u4-batch8/`。

## U4 batch16 — STOP（fresh runtime crash）

已完成並保存三輪：R3-A、CPU-A、CPU-B；各 `11119/11119`
（1519 + 600×16），expected=`00ff0000`，cell exact PASS，oracle PASS；
CPU fallback=1、R3 fallback=0。這三輪的 resource T0/T1/T2/T3 均有記錄且
teardown clean。

第四輪 R3-B 在 workload 開始前，`u4_session.sh` 無法找到 X server PID，
launcher log 寫入新鮮的原生崩潰訊息：

```text
Uctx signo=11 si_code=1 si_addr=0x0000000000000000
PC=0x000000004800226c LR=0x0000007cad92b8e0
Ssig signo=11 code=1 addr=0x0000000000000000
```

沒有 oracle、cell 或 telemetry JSON；`R3-B batch16` 記為 `NOT TESTED`，不是
PASS，也不把這輪當成 batch16 效能結果。ADB crash buffer 目前只讀到較早的
歷史項目，未找到能把這個新 launcher 訊息進一步符號化的同時段 Android
logcat 條目，因此 crash root cause=`UNKNOWN`。按規則不重試、不繼續 U4。

原始證據：`t2-u4-batch16/`，其中 `p2b3a-u4-batch16-r3-b-launcher.log`
為失敗證據。最後狀態已驗證 `NO_X3_RESIDUE`；Stable 未碰。

## U4 狀態

```text
batch8: PASS（correctness / count / endpoint teardown）
batch16: INCONCLUSIVE / BLOCKED（R3-B fresh startup SIGSEGV；3/4 sessions only）
GPU-only/cold U3: completed as recorded in U3 report
Gate A/D/H: NOT AUTHORIZED
```
