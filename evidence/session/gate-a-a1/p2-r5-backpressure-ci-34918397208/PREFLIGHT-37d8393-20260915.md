# Preflight — 37d8393 install + one corrected R5

**EXECUTED 2026-09-15.** Install PASS. R5 hang FALSIFIED; harness counters FAIL.
Do not re-run. Do not silent-retry.
Runtime: `../p2-r3-xpump-runtime/runtime-37d8393/GATE-A-P2-R5-RUNTIME-20260915.md`.

## Binding (local, verified this preflight)

```text
HEAD:     37d839323255b4830d657da3bcbf7f616bc78ad3
CI:       34918397208  https://github.com/waydefu/termux-x11/actions/runs/34918397208
APK:      evidence/session/gate-a-a1/p2-r5-backpressure-ci-34918397208/apk/termux-x11-universal-debug.apk
SIZE:     15233774
SHA256:   31ec7037d3f1dc27836b8719ab21527e2943a21bc2763d08a2dbc51f702c1638 MATCH
version:  1.03.01-37d8393-15.09.26  versionCode 15  package com.waydefu.x11gpu
Build ID: cc8cee05dd7e4ae35085e33dec25bbb2229cee12
fixture:  p_r5_ahb_cycles SHA256 ba3a92d6c97cdce0ad8ca53feb544ff060f4b3f54a12be75969b76cdb319c828
          same ELF as d9b7f60 R5 cells; do not rebuild
device now: still d9b7f60 CI 34872266646  SHA256 255cc37d12…637a2  Build ID 2b02bf13…
```

Installed experimental remains `d9b7f60` until the install script commits.
Historical FAIL cells stay forever:

- `p2-r3-xpump-runtime/runtime-d9b7f60/r5-ahb-cycles/` (X 14858 serial 819)
- `p2-r3-xpump-runtime/runtime-d9b7f60/r5-ahb-cycles-rerun1/` (X 31265 serial 1283)

Fresh cells (empty, ready):

- `p2-r3-xpump-runtime/runtime-37d8393/r0/`
- `p2-r3-xpump-runtime/runtime-37d8393/r5-ahb-cycles/`

Scripts (not executed):

- `p2-r3-xpump-runtime/install-37d8393.sh` — requires `RUN_INSTALL=YES`
- `p2-r3-xpump-runtime/run-r5-corrected.sh` — CELL must be `runtime-37d8393/r5-ahb-cycles`; binds `versionName=1.03.01-37d8393-15.09.26`; 45 s stall / 300 s max watchdog

mDNS at preflight time returned **no** `_adb-tls-connect._tcp.local.` IPv4 pair.
Do **not** hardcode last serial `10.193.235.219:38663`. Live-fetch at execution.

## After authorization — exact sequence

Isolated ADB only: `-H 127.0.0.1 -P 5038`. Never 5037. Never start a second
server on 5038. Never `com.termux.x11`. Never HDMI ops. Screen must be Awake
and unlocked. Kill experimental X only if cmdline starts with
`termux-x11gpu com.waydefu.x11gpu :3`. Force-stop experimental **before**
`pm install-commit`. Stable `:1` PID currently **16085** — must not change.

```bash
MDNS=/root/projects/GPU加速/evidence/session/gate-a-a1/runtime/mdns-address.py
TADB=/data/data/com.termux/files/usr/bin/adb
ADB() {
  env -u ADB_SERVER_SOCKET -u ANDROID_ADB_SERVER_ADDRESS -u ANDROID_ADB_SERVER_PORT \
    HOME=/data/data/com.termux/files/home ANDROID_NO_USE_FWMARK_CLIENT=1 \
    "$TADB" -H 127.0.0.1 -P 5038 -s "$SERIAL" "$@"
}
# live-fetch SERIAL from $MDNS (address= + port=). Then connect if get-state is not device.
RUN=/root/projects/GPU加速/evidence/session/gate-a-a1/p2-r3-xpump-runtime
R0=/root/projects/GPU加速/evidence/session/gate-a-a1/p2-r3-xpump-runtime/runtime-37d8393/r0
R5=/root/projects/GPU加速/evidence/session/gate-a-a1/p2-r3-xpump-runtime/runtime-37d8393/r5-ahb-cycles

RUN_INSTALL=YES SERIAL="$SERIAL" CELL="$R0" bash "$RUN/install-37d8393.sh"
# bind must print INSTALL_37d8393_BIND_PASS
# versionName 37d8393, installed-base SHA256 31ec7037…, Build ID cc8cee05…

SERIAL="$SERIAL" CELL="$R5" bash "$RUN/run-r5-corrected.sh"
```

R5 env (inside script): `TERMUX_X11_GATEA_PROTO=1` `TERMUX_X11_GATEA_TELEMETRY=1`
DEBUG unset. COLD Activity Display0. Fresh `:3`. Holder `p_b3a_hold`. Same
`p_r5_ahb_cycles`.

PASS = checkpoints 1/16/64/256/1024/4096 `fail_acc=0` + fixture RESULT PASS +
N=4096 publish=consume=lookup=draw=fence=completed=success=ack=lease + no hang
+ no `GATEA_FATAL_HALT` / `x-ready-timeout` + teardown `NO_X3_RESIDUE` + Stable
PID unchanged.

## After the one R5 cell

- PASS: write `runtime-37d8393/GATE-A-P2-R5-RUNTIME-20260915.md`, update
  HANDOFF / TEST-MATRIX / runtime handoff. **STOP before R6.** R6–R8 still need
  a separate grant. R7 remains source-blocked (`TERMUX_X11_GATEA_TEST_FAULT`
  absent).
- FAIL: preserve cell, force-stop experimental only, classify, **no silent
  retry**, no C++ patch without a new architecture grant.
- JIT `0x4800xxxx` / dalvik-jit: maps first; OBSERVED; do not patch pump; do
  not silent-retry.

## Do not

Overwrite d9b7f60 R5 cells. Second-retry R5 on `d9b7f60`. Start R6–R10, T2,
Production, PR, origin, Stable/HDMI, PROTO=0 retry, R3 retry. `pkill -f
f8-x11gpu`. `logcat -c`. Run the scripts without the new grant.
